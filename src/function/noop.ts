/**
 * A function that returns always `undefined`.
 * @signature
 *    P.noop()
 * @example
 *    onSomething(P.noop)
 * @category Function
 */
export function noop(): undefined {
  return undefined;
}
